// src/components/AdminDashboard.tsx (stub — will be replaced in Task 11)
export function AdminDashboard({ onSignOut }: { onSignOut: () => void }) {
  return <div style={{ padding: 32, color: '#fff' }}>Dashboard loading…<button onClick={onSignOut}>Sign out</button></div>
}
